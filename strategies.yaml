settings:
  capital: 100000
  port: 8088
  theme: dark

# Public repo: empty by default. Production strategies live in
# strategies.local.yaml (gitignored) or in consumer projects like
# trading-internal/strategies.yaml. `causal-edge init <project>` scaffolds
# a working sma_crossover demo if you want a starting template.
strategies: []
